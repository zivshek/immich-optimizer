tasks:
  - name: perceptual-webp
    command: >
      sh ./transcode-image.sh
      "{{.src_folder}}/{{.name}}.{{.extension}}"
      "{{.dst_folder}}/{{.name}}.webp"
    extensions:
      - jpeg
      - jpg
      - png

  - name: passthrough-images
    command: ""
    extensions:
      - avif
      - bmp
      - dng
      - exr
      - gif
      - heic
      - heif
      - insp
      - jxl
      - pam
      - pfm
      - pgm
      - pgx
      - pnm
      - ppm
      - psd
      - raw
      - rw2
      - svg
      - tif
      - tiff
      - webp
