services:
  immich-optimizer:
    build:
      context: .
      dockerfile: Dockerfile.amd64.goreleaser
    env_file:
      - .env
    volumes:
      - ./watch:/watch
      - ./undone:/undone
      - ./custom_profiles:/custom_profiles
    restart: unless-stopped
